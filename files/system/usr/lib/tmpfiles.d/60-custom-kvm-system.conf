# Cria o diretório de imagens do sistema
d /var/lib/libvirt/images 0755 root root -

# Aplica o atributo No_COW (+C) para desativar Copy-on-Write (Essencial para performance de VMs no BTRFS)
# O 'h' define atributos de arquivo (chattr)
h /var/lib/libvirt/images - - - - +C