---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: dnf
    install:
      packages:
        # - mesa-va-drivers
        # Habilite para garantir aceleração em iGPUs Intel (QuickSync)
        # Não causa conflito em CPUs AMD, apenas ocupa alguns MBs.
        # - intel-media-driver

        # Drivers VDPAU legados (Útil para players de vídeo mais antigos)
        - mesa-vdpau-drivers

        # Multimedia Codecs (GStreamer & Libs)
        - gstreamer1-plugins-bad
        - gstreamer1-plugins-ugly
        # - gstreamer1-plugin-openh264
        - libdvdcss
        # - x264 # Geralmente já vem nas dependências ou flatpak, remova se der conflito
        # - x265 # Idem

        # Ferramenta essencial para manipular mídia via terminal
        # - ffmpeg
