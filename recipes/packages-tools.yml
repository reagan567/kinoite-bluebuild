# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: dnf
    repos:
      cleanup: true
      copr:
        - atim/starship
        - lilay/topgrade

    install:
      packages:
        - starship
        - topgrade

  - type: dnf
    install:
      packages:
        - fastfetch
        - distrobox
