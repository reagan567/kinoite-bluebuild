name: build-nvidia
on:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  bluebuild:
    name: Build Standard Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    strategy:
      fail-fast: true
      matrix:
        recipe:
          - recipe-nvidia.yml
    steps:
      # 0. CHECKOUT (Essencial para ler o timestamp do Git)
      - name: Checkout repo
        uses: actions/checkout@v6
        with:
          fetch-depth: 0 # Baixa o histórico para pegar a data correta

      # 1. TRAVAR TIMESTAMP (A Solução para o Cache)
      - name: Set reproducible build timestamp
        id: timestamp
        run: |
          # Define a data do build como a data do último commit
          COMMIT_TIME=$(git log -1 --pretty=%ct)
          echo "SOURCE_DATE_EPOCH=$COMMIT_TIME" >> $GITHUB_ENV
          echo "Timestamp travado em: $COMMIT_TIME"

      # 2. Otimização de espaço (Mantive o seu original)
      - name: Optimize build times
        shell: bash
        run: |
          echo 'set man-db/auto-update false' | sudo debconf-communicate
          sudo dpkg-reconfigure man-db
          sudo rm -f /var/lib/man-db/auto-update
          sudo sed -i 's/^update_initramfs=.*/update_initramfs=no/' /etc/initramfs-tools/update-initramfs.conf

      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          remove-docker-images: 'true'

      # 3. BUILD COM CORREÇÕES
      - name: Build Standard Image
        uses: blue-build/github-action@v1.11
        env:
          # Garante que o buildah veja a variável de tempo
          SOURCE_DATE_EPOCH: ${{ env.SOURCE_DATE_EPOCH }}
        with:
          recipe: ${{ matrix.recipe }}
          cosign_private_key: ${{ secrets.SIGNING_SECRET }}
          registry_token: ${{ github.token }}
          pr_event_number: ${{ github.event.number }}
          maximize_build_space: true

          # MUDANÇA CRÍTICA: Habilite isso para ter deltas (Zstd Chunked)
          build_chunked_oci: false

          max_layers: 128
          use_cache: true

          # OBSERVAÇÃO SOBRE SQUASH:
          # Com chunked: true, o squash é aceitável.
          # Sem chunked, o squash obriga o download total da imagem.
          squash: false

          retry_push_count: 3
          verify_install: true